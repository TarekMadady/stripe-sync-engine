{
  "version": 2,
  "builds": [{
    "src": "./dist/server.js",
    "use": "@now/node"
  }],
  "routes": [{"handle": "filesystem"},
    {
      "src": "/.*",
      "dest": "./dist/server.js"
    }
  ]
}
